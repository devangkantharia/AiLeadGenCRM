// Location: /app/(app)/deals/page.tsx
import React from "react";

export default function DealsPage() {
    return (
        <div>
            <h1 className="text-2xl font-bold">Deals</h1>
            <p>This is where the deals pipeline (kanban or list) will go.</p>
        </div>
    );
}